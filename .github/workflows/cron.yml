name: cron test

on:
  schedule:
  - cron:  '*/60 * * * *'

jobs:
  stale:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [13.x]
    
    steps:
    - uses: actions/checkout@master

    - name: setup node
      uses: actions/setup-node@v1
      with:
        node-version: '13.x'
    
    - name: install
      run : yarn

    - name: notify
      run : node app.js ${{secrets.NOTIFY_TEST_TOKEN}}